// FUNCTION

// thành phần của 1 function
// từ khoá function, tham số trong hàm, body xử lý trong hàm, return

// example
function sumTwo(a, b) {
  return a + b;
}

// Function Expression -  Hàm biểu thức

const sayHi = function (name) {
  console.log("xin chào" + name);
};

sayHi("Ninh");
